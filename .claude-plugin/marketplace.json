{
  "name": "open-context",
  "description": "Marketplace for the open-context plugin: zero-LLM context resolver for Rails HMVC codebases.",
  "owner": {
    "name": "oopsla5xx"
  },
  "plugins": [
    {
      "name": "open-context",
      "source": ".",
      "description": "Run `/oc-setup` once, then every prompt automatically gets the right context injected — domains, component chain, files, and architecture rules — based on your project's context.yaml."
    }
  ]
}
